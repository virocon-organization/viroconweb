language: python
python:
  - "3.6"

before_install:
- "sudo apt-get update && sudo apt-get install --no-install-recommends texlive-fonts-recommended texlive-latex-extra texlive-fonts-extra texlive-latex-recommended dvipng"

script:
- mkdir _build
- python manage.py test
- coverage run --source=contact,EngineeringApp,enviro,home,user  manage.py test
- bash ./deploy_docs.sh

after_success: coveralls

env:
    global:
    - ENCRYPTION_LABEL: "2d371be78117"
    - COMMIT_AUTHOR_EMAIL: "virocon@uni-bremen.de"
    

notifications:
  email:
    recipients:
      - a.haselsteiner@uni-bremen.de
      - kawindme@uni-bremen.de
      - janlehmk@uni-bremen.de
      - to_pa@uni-bremen.de
    on_success: change
    on_failure: always
